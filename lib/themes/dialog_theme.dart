import '../../lib_exp.dart';

class DialogLibTheme {
  static dialogTitleStyle(Color color) => TextStyle(
        color: color,
        fontSize: 20,
        fontWeight: FontWeight.bold,
      );
  static dialogDescriptionStyle(Color color) => TextStyle(
        color: color,
        fontSize: 11,
        fontWeight: FontWeight.normal,
      );
}
